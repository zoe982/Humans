export const base = "";
export const assets = "";

export function resolve(path: string): string {
  return path;
}

export function resolveRoute(id: string, _params: Record<string, string>): string {
  return id;
}
