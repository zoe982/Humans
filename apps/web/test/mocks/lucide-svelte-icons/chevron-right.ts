/**
 * Stub for lucide-svelte/icons/* sub-path imports.
 * Used by shadcn-svelte components (BreadcrumbSeparator, etc.).
 *
 * Re-exports the same icon-stub.svelte component used by the main
 * lucide-svelte mock so SVG elements are never created in happy-dom.
 */
import IconStub from "../icon-stub.svelte";
export default IconStub;
